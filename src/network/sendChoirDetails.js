import makeRequest from "./makeRequest";

export default function sendChoir(choirId) {

  console.log("saved!");
  // return await makeRequest(`${apiUrl}/choirs/${choirId}`, "json", {
  //   method: "PATCH",
  //   body: publishData,
  //   headers: { Authorization: `Bearer ${authToken}` },
  //   signal: abortSignal,
  //   timeout: 3000
  // })
}